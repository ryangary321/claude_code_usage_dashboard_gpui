use gpui::*;

// Placeholder for table components
pub struct ModelTable;
pub struct ProjectTable; 
pub struct SessionTable;

impl ModelTable {
    pub fn new() -> Self {
        Self
    }
}

impl ProjectTable {
    pub fn new() -> Self {
        Self
    }
}

impl SessionTable {
    pub fn new() -> Self {
        Self
    }
}